server:
  port: 6060

spring:
  datasource:
    url: jdbc:mysql://${FACTURACION_DB_HOST:mysql-facturacion}:${FACTURACION_DB_PORT:3306}/${FACTURACION_DB_NAME:db_hotel_facturacion}?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true
    username: ${FACTURACION_DB_USERNAME:root}
    password: ${FACTURACION_DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://registry-server:7081/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  prometheus:
    metrics:
      export:
        enabled: true

jwt:
  secret: ${JWT_SECRET}
  issuer: auth
